-- Ducks CRM v2.27 - Dos tutores por jugador
-- Ejecutar en Supabase SQL Editor.

alter table public.players
  add column if not exists tutor_2 text;

alter table public.players
  add column if not exists tutor_phone_2 text;

select 'OK - Ducks CRM v2.27 columnas de tutores agregadas correctamente' as resultado;
